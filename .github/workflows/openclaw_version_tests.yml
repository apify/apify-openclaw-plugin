name: OpenClaw Version Test

on:
  pull_request:
    branches: [main]
  schedule:
    # Mondays 07:00 UTC — 8am Prague in winter (CET), 9am Prague in summer (CEST).
    - cron: "0 7 * * 1"
  workflow_dispatch:

concurrency:
  group: openclaw-version-test-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  discover:
    runs-on: ubuntu-latest
    outputs:
      versions: ${{ steps.filter.outputs.versions }}
    steps:
      - uses: actions/checkout@v4

      # Keep this job on the same Node major as the matrix `test` job below
      # (Node 24) for consistency; this job only runs `npm view`, so the exact
      # version is not load-bearing here.
      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - id: filter
        name: Pick latest 3 OpenClaw versions
        shell: bash
        run: |
          set -euo pipefail

          # Delegated to scripts/openclaw-version.mjs so this matrix, the publish
          # gate and the bump script share one definition of an OpenClaw release.
          # It drops true pre-releases (-alpha.N / -beta.N) but KEEPS the numeric
          # re-release suffix openclaw uses for respins (e.g. 2026.7.1-2), which
          # `latest` can point at — a plain `select(test("-") | not)` filter used
          # to hide exactly the version most users install.
          json=$(node scripts/openclaw-version.mjs matrix 3 | tail -n 1)

          echo "versions=$json" >> "$GITHUB_OUTPUT"
          echo "Selected versions: $json"

  test:
    needs: discover
    runs-on: ubuntu-latest
    name: OpenClaw ${{ matrix.version }}
    strategy:
      fail-fast: false
      matrix:
        version: ${{ fromJson(needs.discover.outputs.versions) }}
    steps:
      - uses: actions/checkout@v4

      # OpenClaw's newer releases raise their Node engine floor over time
      # (2026.9.3+ require Node >=24.16.0 <25 || >=26.1.0, dropping Node 22).
      # Run the matrix on Node 24 so `npm install openclaw@<matrix version>`
      # satisfies every currently-selected release's `engines.node` — the
      # intersection of the latest matrix versions' ranges is >=24.16.0 <25.
      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install OpenClaw ${{ matrix.version }} and smoke-test plugin
        shell: bash
        run: |
          set -euo pipefail

          # Plugin id is centralized in openclaw.plugin.json (single source of
          # truth). Derive it from the checked-out manifest instead of hardcoding.
          PLUGIN_ID=$(jq -r .id "$GITHUB_WORKSPACE/openclaw.plugin.json")
          echo "Plugin id (from manifest): $PLUGIN_ID"

          # On PRs, pack the PR's source and install that tarball. On schedule /
          # manual dispatch, install the currently-published @latest from npm.
          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            echo "::group::npm install && npm pack (PR branch)"
            cd "$GITHUB_WORKSPACE"
            npm install
            tarball=$(npm pack | tail -n 1)
            PLUGIN_SPEC="$GITHUB_WORKSPACE/$tarball"
            echo "Packed: $PLUGIN_SPEC"
            echo "::endgroup::"
          else
            PLUGIN_SPEC="@apify/apify-openclaw-plugin@latest"
            echo "Using published spec: $PLUGIN_SPEC"
          fi

          WORKDIR="$RUNNER_TEMP/openclaw-test"
          mkdir -p "$WORKDIR"
          cd "$WORKDIR"

          echo "::group::npm install openclaw@${{ matrix.version }}"
          npm init -y >/dev/null
          npm install "openclaw@${{ matrix.version }}"
          echo "::endgroup::"

          # OpenClaw 2026.8.1 hardened `plugins install` with two non-interactive
          # gates that abort in CI (no TTY to prompt):
          #   * `--force`               — confirm a non-ClawHub source (our tarball /
          #                               `@latest` npm spec are both outside ClawHub).
          #   * `--accept-capabilities` — consent to the plugin's declared capabilities.
          # Older matrix versions (e.g. 2026.6.34, 2026.7.1-2) DON'T know
          # `--accept-capabilities` and hard-fail with "OpenClaw does not recognize
          # option ..." if it's passed. So we feature-detect each flag from the
          # install `--help` text and only pass what the installed version supports.
          # This self-adjusts as the matrix rotates forward — no version table to keep.
          install_help=$(npx --no-install openclaw plugins install --help 2>&1 || true)
          install_flags=()
          if grep -q -- '--force' <<<"$install_help"; then
            install_flags+=("--force")
          fi
          if grep -q -- '--accept-capabilities' <<<"$install_help"; then
            install_flags+=("--accept-capabilities")
          fi

          echo "::group::openclaw plugins install $PLUGIN_SPEC ${install_flags[*]}"
          npx --no-install openclaw plugins install "$PLUGIN_SPEC" "${install_flags[@]}"
          echo "::endgroup::"

          # `plugins list` is echoed for diagnostics only — its formatted table
          # word-wraps long ids/paths across cells, so any substring grep on it
          # is brittle. The real load check is `plugins inspect` below, which
          # exits non-zero if the plugin id isn't registered and returns JSON
          # we can structurally assert on.
          echo "::group::openclaw plugins list"
          npx --no-install openclaw plugins list || true
          echo "::endgroup::"

          echo "::group::openclaw plugins inspect $PLUGIN_ID --runtime --json"
          inspect_json=$(npx --no-install openclaw plugins inspect "$PLUGIN_ID" --runtime --json)
          echo "$inspect_json" | jq .
          if ! echo "$inspect_json" | jq -e '.. | strings | select(. == "apify")' >/dev/null; then
            echo "FAIL: 'apify' tool not found in plugin runtime inspect output" >&2
            exit 1
          fi
          echo "::endgroup::"

          echo "OK: plugin loaded and apify tool is registered on openclaw ${{ matrix.version }}"

  # Stable aggregator check — mark THIS one as required in branch protection.
  # The matrix job names ("OpenClaw 2026.5.X") rotate as discovery picks up new
  # versions, but this job's name never changes.
  required:
    needs: [discover, test]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: All OpenClaw version tests must pass
        shell: bash
        run: |
          if [[ "${{ needs.discover.result }}" != "success" || "${{ needs.test.result }}" != "success" ]]; then
            echo "discover=${{ needs.discover.result }} test=${{ needs.test.result }}"
            echo "One or more OpenClaw version tests failed."
            exit 1
          fi
          echo "All OpenClaw version tests passed."

  # Slack notification on scheduled failure — disabled for now.
  # To re-enable: uncomment this job and set the `SLACK_WEBHOOK_URL` repo secret.
  #
  # notify:
  #   needs: test
  #   if: always() && needs.test.result == 'failure' && github.event_name == 'schedule'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Notify Slack on scheduled failure
  #       env:
  #         SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
  #         RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
  #       shell: bash
  #       run: |
  #         if [ -z "${SLACK_WEBHOOK_URL:-}" ]; then
  #           echo "SLACK_WEBHOOK_URL secret not configured — skipping notification."
  #           exit 0
  #         fi
  #
  #         payload=$(jq -nc \
  #           --arg text "🚨 *OpenClaw Version Test failed* (scheduled run)
  #         One or more OpenClaw versions failed the plugin install smoke test.
  #         <${RUN_URL}|View run details>" \
  #           '{text: $text}')
  #
  #         curl -sS -X POST -H 'Content-Type: application/json' \
  #           --data "$payload" "$SLACK_WEBHOOK_URL"
